---
title: Swipe to Confirm
description: A customizable swipe gesture component for confirming or dismissing actions
category: Interaction
keywords: ["swipe", "gesture", "confirm", "dismiss", "interactive"]
---

import { SwipeToConfirm } from "@/data/components/Swipe/SwipeToConfirm";
import { ComponentPreview } from "@/components/docs/ComponentPreview";
import { CodePreview } from "@/components/docs/CodePreview";

import { ThemeSwitcher } from "@/components/docs/ThemeSwitcher";
import {
  Check,
  X,
  Archive,
  Trash2,
  Heart,
  ShoppingCart,
  Star,
  Power,
  Lock,
  Unlock,
  Volume2,
  VolumeX,
  Send,
  Download,
} from "lucide-react";
import SwipeUseCasesDemo from "@/data/components/Swipe/SwipeToConfirmDemo";

<div className="mb-6 flex justify-end">
  <ThemeSwitcher />
</div>

## Component Code

<CodePreview
  componentName="SwipeToConfirm"
  code={`"use client";

import { motion, useMotionValue, useTransform, animate } from "motion/react";
import { useEffect, useState } from "react";
import { Check, X } from "lucide-react";

export function SwipeToConfirm({
  onConfirm,
  onDismiss,
  confirmText = "Confirm",
  dismissText = "Dismiss",
  neutralText = "Swipe to action",
  confirmIcon: ConfirmIcon = Check,
  dismissIcon: DismissIcon = X,
  confirmColor = "green",
  dismissColor = "red",
  size = "default",
}) {
  // ... component implementation
}`}
  props={[
    {
      name: "onConfirm",
      type: "() => void",
      required: false,
      description: "Callback function when swiped right",
    },
    {
      name: "onDismiss",
      type: "() => void",
      required: false,
      description: "Callback function when swiped left",
    },
    {
      name: "confirmText",
      type: "string",
      required: false,
      defaultValue: "'Confirm'",
      description: "Text shown when confirming",
    },
    {
      name: "dismissText",
      type: "string",
      required: false,
      defaultValue: "'Dismiss'",
      description: "Text shown when dismissing",
    },
    {
      name: "neutralText",
      type: "string",
      required: false,
      defaultValue: "'Swipe to action'",
      description: "Text shown in neutral state",
    },
    {
      name: "confirmColor",
      type: "string",
      required: false,
      defaultValue: "'green'",
      description: "Color for confirm action",
    },
    {
      name: "dismissColor",
      type: "string",
      required: false,
      defaultValue: "'red'",
      description: "Color for dismiss action",
    },
    {
      name: "size",
      type: "'small' | 'default' | 'large'",
      required: false,
      defaultValue: "'default'",
      description: "Size of the component",
    },
  ]}
  withA11yCheck={true}
>
  <SwipeToConfirm
    confirmText="Confirm"
    dismissText="Dismiss"
    neutralText="Swipe to action"
  />
</CodePreview>

## Use Cases

### Notification Management

<CodePreview
  componentName="NotificationSwipe"
  code={`
<SwipeToConfirm
  confirmText="Archive"
  dismissText="Delete"
  neutralText="Swipe to manage"
  confirmIcon={Archive}
  dismissIcon={Trash2}
  confirmColor="blue"
  dismissColor="red"
  onConfirm={() => console.log("Archived")}
  onDismiss={() => console.log("Deleted")}
/>
  `}
>
  <SwipeUseCasesDemo />
</CodePreview>
