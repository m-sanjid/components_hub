---
title: Tabs
description: A customizable tabs component with micro-interactions and various states
category: Navigation
keywords: ["tabs", "navigation", "switching", "animated", "interactive"]
---

<CodePreview name="tabs" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli"><Shadcn /></TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Steps>

<Step>Use the following command to install the component:</Step>

```bash
npx shadcn@latest add https://ui.sanjid.shop/r/tabs.json
```

</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install motion
```

<Step>Add util file</Step>

##### lib/utils.ts

```tsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="tabs" title="components/ui/tabs.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Props

<PropsTable
  props={[
    {
      name: "value",
      type: "string",
      description: "The value of the tab to display",
    },
    {
      name: "label",
      type: "string",
      description: "The label of the tab to display",
    },
    {
      name: "content",
      type: "React.ReactNode",
      description: "The content of the tab to display",
    },
  ]}
/>

<ExampleShowcase name="tabs-vertical" />
<ExampleShowcase name="tabs-with-icon" />
<ExampleShowcase name="controlled-tabs" />
