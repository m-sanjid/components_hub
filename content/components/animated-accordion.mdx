---
title: Animated Accordion
description: A beautiful and interactive accordion component with smooth animations and customizable content
category: Display
keywords: ["accordion", "expandable", "animated", "interactive", "collapsible"]
---

<CodePreview
  name="animated-accordion"
  title="Animated Accordion"
  description="A beautiful and interactive accordion component with smooth animations and customizable content"
  props={[
    {
      name: "items",
      type: "array",
      description: "The items to display in the accordion",
      required: true,
    },
    {
      name: "type",
      type: "string",
      description: "The type of accordion to display",
      required: false,
      defaultValue: "single",
    },
    {
      name: "defaultOpen",
      type: "array",
      description: "The items to display in the accordion",
      required: false,
      defaultValue: "[]",
    },
    {
      name: "className",
      type: "string",
      description: "The class name to apply to the accordion",
      required: false,
      defaultValue: "",
    },
  ]}
/>

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add animated-accordion --component
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install motion lucide-react
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="animated-accordion"
  title="components/ui/animated-accordion.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { AnimatedAccordion } from "@/components/ui/animated-accordion";
```

```tsx showLineNumbers
<AnimatedAccordion />
```
