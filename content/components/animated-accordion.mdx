---
title: Accordion
description: A beautiful and interactive accordion component with smooth animations and customizable content
category: Display
keywords: ["accordion", "expandable", "animated", "interactive", "collapsible"]
---

<CodePreview name="animated-accordion" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">
    <Shadcn />
  </TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.sanjid.shop/r/accordion.json
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install motion lucide-react
```

<Step>Add util file</Step>

##### lib/utils.ts

```tsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource
  name="animated-accordion"
  title="components/ui/animated-accordion.tsx"
/>

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Props

<PropsTable
  props={[
    {
      name: "items",
      type: "array",
      description: "The items to display in the accordion",
      required: true,
    },
    {
      name: "allowMultiple",
      type: "boolean",
      description: "Allow multiple items to be open at the same time",
      required: false,
      defaultValue: "false",
    },
    {
      name: "defaultOpen",
      type: "string[]",
      description: "The default open items",
      required: false,
      defaultValue: "[]",
    },
    {
      name: "className",
      type: "string",
      description: "The class name to apply to the accordion group",
      required: false,
      defaultValue: "",
    },
  ]}
/>

## Usage

```tsx showLineNumbers
import { Accordion, AccordionGroup } from "@/components/ui/animated-accordion";
```

```tsx showLineNumbers
<AccordionGroup
  items={[
    {
      id: "accordion-1",
      title: "Accordion 1",
      content: "Content 1",
    },
    {
      id: "accordion-2",
      title: "Accordion 2",
      content: "Content 2",
    },
  ]}
/>
```
